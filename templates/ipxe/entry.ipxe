#!ipxe

set server {{ provisioning_server }}

echo ==================================================
echo Graystone AI Forge
echo Provisioning client
echo MAC: ${net0/mac}
echo ==================================================
echo

chain http://${server}/forge/boot-mac?mac=${net0/mac}
