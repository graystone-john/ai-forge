---
- name: Validate AI Node
  hosts: daedalus
  gather_facts: true
  become: true

  vars_files:
    - "../profiles/inference/{{ inference_profile }}.yaml"

  pre_tasks:
    - name: Report AI Forge stage start
      ansible.builtin.debug:
        msg: "AI_FORGE_STAGE_START stage=5 name=validation machine={{ inventory_hostname }}"

  roles:
    - validate-coding-node

  post_tasks:
    - name: Report AI Forge stage complete
      ansible.builtin.debug:
        msg: "AI_FORGE_STAGE_COMPLETE stage=5 name=validation machine={{ inventory_hostname }}"
