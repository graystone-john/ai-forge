---
- name: Configure AI Node agent credentials
  hosts: daedalus
  gather_facts: true
  become: true

  pre_tasks:
    - name: Report AI Forge step start
      ansible.builtin.debug:
        msg: "AI_FORGE_STEP_START name=agent-credentials machine={{ inventory_hostname }}"

  roles:
    - agent-credentials

  post_tasks:
    - name: Report AI Forge step complete
      ansible.builtin.debug:
        msg: "AI_FORGE_STEP_COMPLETE name=agent-credentials machine={{ inventory_hostname }}"
