---
- name: Validate Hermes image integration
  hosts: local_ai_coding_node
  become: true
  gather_facts: false

  vars_files:
    - "../profiles/inference/{{ inference_profile }}.yaml"

  roles:
    - role: ai-forge
      vars:
        ai_forge_stage: validate-hermes-agent
